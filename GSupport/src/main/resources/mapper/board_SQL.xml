<?xml version="1.0"	encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper	namespace="board">

	<select	id="boardList" resultType="HashMap">
		SELECT * FROM board_view limit #{startPage}, #{lastPage}
	</select>
	
	<select	id="categoryList" resultType="HashMap">
		SELECT * FROM board_cate
	</select>
	
	<select	id="board" parameterType="HashMap" resultType="HashMap">
		SELECT * FROM board_view limit #{startPage}, #{lastPage}
	</select>
	
	<select	id="totalCount" parameterType="HashMap" resultType="HashMap">
		SELECT COUNT(*) as totalCount FROM board_view
	</select>
	
</mapper>